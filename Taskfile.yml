version: "3"

tasks:
  ui:dev:
    desc: Start the UI dev server
    dir: ui
    cmd: bun run dev

  ui:build:
    desc: Build the UI for production
    dir: ui
    cmd: bun run build

  dev:
    desc: Start the Tauri dev environment
    dir: crates/shade-tauri
    cmd: cargo tauri dev
